2. File parser

prompt is fucked for some reason, no idea what's wrong
  FIX: scanf(" %c", &decision);
The %c conversion specifier won't automatically skip any leading whitespace, so if there's a stray newline in the input stream (from a previous entry, for example) the scanf call will consume it immediately.


// edit student:
  1. If a student is not in the database, re-enter the desired student number or exit with 'button'

9. Программа должна обнаруживать ошибки, вызванные неисправностями жесткого диска, отсутствием на нем свободного места и др

13.  При добавлении информации о новом студенте должны выделяться дополнительная память, а при удалении информации — память должна освобождаться


Algorithm:
- Asks whether you want to read from a file or create new database

  Create new database:
    - Reads file line 1 - number of students - writes to stud_num
    - Reads students
    - Asks for actions: 
      3. Ввод, корректировка и удаление информации о студенте
      4. Поиск информации в базе данных по заданным фамилии, имени и отчеству студента
      5. Поиск информации о студентах, которым на момент поиска исполнилось заданное количество лет
      6. Вывод на экран монитора всей информации из базы данных
      7. Вывод на экран монитора найденной информации из базы данных
      8. Вывод на печатающее устройство всей информации из базы данных
      https://youtu.be/GDxLs0CEdAY
      Windows:
        system("notepad /p file_name.txt");